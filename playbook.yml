---
- hosts: localhost
  vars_files:
    - ./config.yml
    - vars/main.yml

  become: yes
  connection: local
  tasks:

    - include: tasks/common_set.yml

    - include: tasks/install.yml

    - include: tasks/create_nfs.yml
      tags: after_task

  handlers:
    - include: handlers/main.yml

